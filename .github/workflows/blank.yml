name: jagadeesh
on: 
  push:
    branches:
      - main 
jobs:
  one:
    runs-on: ubuntu-latest
    steps:
      - name: build  
        run: echo "my password ${{ secrets.my_secret }}"
         
    
